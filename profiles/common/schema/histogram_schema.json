{
"title": "I3S building scene layer: histogram",
    "type": "object",
    "$schema": "http://json-schema.org/draft-04/schema",
    "description": "The histogram of the point cloud scene layer. The bin size may be computed as (max-min)/bin count. Please note that stats.histo.min/max is not equivalent to stats.min/max since values smaller than stats.histo.min and greater than stats.histo.max are counted in the first and last bin respectively. The values stats.min and stats.max may be conservative estimates.",
    "properties": {
        "minimum": {
            "type": "number",
            "description": "Minimum attribute value for the entire layer." 
        },   
        "maximum": {
            "type": "number",
            "description": "Maximum attribute value for the entire layer. Maximum array size for stats.histo.counts is 256." 
        },  
        "counts": {
            "type": "array",
            "items": {
                "type": "number"
            },
            "description": "Count for the entire layer." 
        } 
    },
    "required": [
        "minimum",
        "maximum",
        "counts"        
    ],
    "esriDocumentation": {
        "examples": [
           {
             "title": "histogram",
             "code": {
                "histogram": 
                    {
                        "minimum": 1567.596482, 
                        "maximum": 1644.937967, 
                        "counts": [1, 1, 0, 0, 0, 1, 3, 123, 1852]
                        }
                    }
                }
            ]
        }
    }



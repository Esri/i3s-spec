{
  "title": "FeatureID attribute",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "FeatureID helps to indentify part of mesh from belonging to a particular GIS `feature`. This ID may be used to query additional information from a `FeatureService`. _TBD_: meaning when topology is `point` or `line` ",
  "properties": {
    "type": {
      "type": "string",
      "enum": [ "UInt16", "UInt32", "UInt64" ],
      "description": "A feature integer ID"
    }, 
    "component": {
      "type": "integer",
      "description": "must be 1"
    },
    "encoding": {
      "type": "string",
      "enum": [ "none" ]
    },
    "binding": {
      "type": "string",
      "enum": [ "per-feature" ],
      "enum-description": {
        "per-feature": "Default for `geometryBuffer.featureId`. One `feature_id` per feature. **Requirement**: a) [`FaceRange`](geometryfacerange.md) attribute must be present** to map features-to-faces and vertices must _be grouped by feature_ **OR** b) [`compressedAttribute.attributes`](compressedAttributes.md) has `uvRegionIndex`. Important: a) and b) are mutually exlusive"
      }
    }
  },
  "required": [
    "type",
    "component"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "Feature_id less than 65k, per-feature. `faceRange` attribute will be required",
        "code": {
          "featureId": {
            "type": "UInt16",
            "component": 1
          }
        }
      }
    ]
  }
}
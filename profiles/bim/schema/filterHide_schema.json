{
  "title": "Filter style hide",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "Hides all features that do not comply to the filter of the BIM Building Scene Layer.",
  "properties": {
    "type": {
        "type": "string",
        "enum": [
          "hide"
        ],
      "description": "Declares filter style of type hide."
    }
  }
}

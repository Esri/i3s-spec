{
  "title": "filterAuthoringInfo [building profile]",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "The filter authoring info object contains metadata about the authoring process for creating a filter object. This allows the authoring client to save specific, overridable settings.  The next time it is accessed with an authoring client, the selections are remembered. Non-authoring clients can ignore it.",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "checkbox"
      ],
      "enum-description": {
        "checkbox": "Client UI with checkbox representation for each filter type and filter value."
      },
      "description": "Declares type or filter authoring info."
    },
    "filterBlocks": {
      "type": "array",
      "items": {
        "type": "object",
        "$ref": "filterBlockAuthoringInfo.bld.0106.json"
      },
      "description": "Array of filter block authoring info."
    }
  },
  "required": [
    "type",
    "filterBlocks"
  ],
  "additionalProperties": false,
  "esriDocumentation": {
    "examples": [
      {
        "title": "Building Scene Layer filter authoring info",
        "code": {
          "type": "checkbox",
          "filterBlocks": [
            {
              "filterTypes": [
                {
                  "filterType": "BldgLevel",
                  "filterValues": [
                    "3"
                  ]
                },
                {
                  "filterType": "CreatedPhase",
                  "filterValues": [
                    "1"
                  ]
                }
              ]
            }
          ]
        }
      }
    ]
  }
}
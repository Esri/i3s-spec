{
  "title": "Filter style",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "Filter style applied to the feature if filter is present.",
  "properties": {
    "name": {
        "type":"string",
        "description": "Name of the filter style"
    }
    },
  "required": [
    "name"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "Building Scene Layer filter style",
        "code": ""
      }
    ]
  }
}

{
  "title": "Filter",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "The filter object can be applied to a building scene layer. Filter allows client applications to reduce the drawn elements of a building to specific filter types and values.",
  "properties": {
    "id": {
      "type": "number",
      "description": "Unique identifier of the filter."
    },
    "name": {
      "type": "string",
      "description": "Name of the filter."
    },
    "description": {
      "type": "string",
      "description": "Description of the filter."
    },
    "modelName": {
      "type": "string",
      "description": "Model name of the filter. Used by client applications to define special behavior. For example, the modelName 'currentState' describes the default filter for all ArcGIS client applications expect a building to have a current state. In the 'currentState' filter, all elements in the 'created' phases are drawn, while elements in the 'demolished' phases are invisible. can build specific UI for this filter."
    },
    "filterBlocks":{
      "type": "array",
      "items": {
        "type": "object",
        "$ref": "filterBlock_schema.json"
      },
      "description": "Array of filter blocks defining the filter. A filter contains at least one filter block."
    }
  },
  "required": [
    "id",
    "name",
    "description",
    "filterBlocks"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "Building Scene Layer",
        "code": ""
      }
    ]
  }
}


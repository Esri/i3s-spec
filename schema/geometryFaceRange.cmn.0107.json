{
  "title": "Face Range",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description-href": "description/geometryFaceRange.cmn.0107.md",
  "properties": {
    "type": {
      "type": "string",
      "description": "Data type for the index range",
      "enum": [
        "UInt32"
      ]
    },
    "component": {
      "type": "integer",
      "description": "Pair of indices marking first and last triangles for a feature.",
      "enum": [
        2
      ]
    },
    "encoding": {
      "type": "string",
      "enum": [
        "none"
      ]
    },
    "binding": {
      "type": "string",
      "enum": [
        "per-feature"
      ]
    }
  },
  "required": [
    "type",
    "component"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "FaceRange",
        "code": {
          "type": "UInt32",
          "component": 2
        }
      }
    ]
  }
}
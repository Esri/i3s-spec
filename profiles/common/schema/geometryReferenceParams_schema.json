{
  "title": "GeometryReferenceParams",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "Instead of owning a Geometry exclusively, a Feature can reference part of a Geometry defined for the node. This allows to pre-aggregate Geometries for many features. In this case, GeometryReferenceParams must be used.",
  "properties": {
    "href": {
      "type": "string",
      "description": "In-document absolute reference to full geometry definition (Embedded or ArrayBufferView) using the I3S json pointer syntax. For example /geometryData/1"
    },
    "type": {
      "type": "string",
      "description": "The type denotes whether the following geometry is defined by using array buffer views (ArrayBufferView), as an internal reference (GeometryReference), as a reference to a shared Resource (SharedResourceReference) or embedded (Embedded)."
    },
    "faceRange": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Inclusive range of faces in this geometry that belongs to this feature."
    },
    "lodGeometry": {
      "type": "boolean",
      "description": "True if this geometry participates in an LoD tree. Always true in mesh-pyramids profile."
    }
  },
  "required": [
    "href"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "info for point scene layer"
      },
      {
        "title": " info for 3D object scene layer",
        "code": {
          "type": "GeometryReference",
          "params": {
            "$ref": "/geometryData/1",
            "faceRange": [
              0,
              195
            ]
          }
        }
      },
      {
        "title": " info for integrated mesh scene layer",
        "code": {
          "type": "GeometryReference",
          "params": {
            "$ref": "/geometryData/13752",
            "faceRange": [
              0,
              3526
            ]
          }
        }
      }
    ]
  }
}
{
  "title": "Node pages definition",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description-href": "description/nodepages.cmn.0107.md",
  "related": "node.cmn.0107",
  "properties": {
    "nodesPerPage": {
      "type": "integer",
      "description": "Number of nodes per page for this layer. **Must be a power-of-two** less than `4096`"
    },
    "rootIndex": {
      "type": "integer",
      "description": "Index of the root node.  Default = 0."
    },
    "lodSelectionMetricType": {
      "type": "string",
      "enum": [
        "maxScreenThresholdSQ"
      ],
      "enum-description": {
        "maxScreenThresholdSQ": "A calculated value used to help with better view frustum culling and increases benefits from the non-isotropic screen projected size. `maxScreenThresholdSQ = PI * 0.25 * maxScreenThreshold * maxScreenThreshold`"
      },
      "description": "Defines the meaning of `nodes[].lodThreshold` for this layer."
    }
  },
  "required": [
    "nodesPerPage",
    "lodSelectionMetricType"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "64 nodes per page index",
        "code": {
          "nodesPerPage": 64,
          "rootIndex": 0,
          "lodSelectionMetricType": "maxScreenThresholdSQ"
        }
      }
    ]
  }
}
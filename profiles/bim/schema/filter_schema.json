{
  "title": "Filter",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "The filter object which can be applied to a building scene layer.",
  "properties": {
    "id": {
      "type": "number",
      "description": "Unique identifier of the filter."
    },
    "name": {
      "type": "string",
      "description": "Name of the filter."
    },
    "description": {
      "type": "string",
      "description": "Description of the filter."
    },
    "modelName": {
      "type": "string",
      "description": "Model name defines the filter. For example, if the modelName is Floor or Building the clients can build specific UI for this filter. Any other modelName is custom. This would be important for web scene viewer so they can identify filter that are not floor or others that they will have UI for."
    },
    "filterMode": {
      "type": "object",
      "description": "Filter mode defines how features are drawn. For example, the filter mode of a filter can be wire frame.",
      "$ref":"filterMode_schema.json"
    },
    "filterExpression": {
      "type": "string",
      "description": "Filter query expression for a building scene layer."
    },
    "filterAuthoringInfo": {
      "description": "Authoring info used to generate user interface for authoring clients.",
      "$ref": "filterAuthoringInfo_schema.json"
    }
  },
  "required": [
    "id",
    "name",
    "description",
    "modelName",
    "filterMode",
    "filterExpression"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "Building Scene Layer",
        "code": ""
      }
    ]
  }
}


{
  "title": "BIM attribute statistics",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "Concatenated attribute statistics . If needed, the type of the attribute (string or number) may be inferred from `mostFrequentValues` and/or `min`/`max` fields",
  "properties": {
    "fieldname": {
      "type": "string",
      "description": "Name of the field."
    },
    "label": {
      "type": "string",
      "description": "Label of the field name. If label is empty, the label and fieldName are identical. Labels are shown in the UI for filter types."
    },
    "modelName": {
      "type": "string",
      "enum": [ "floorId", "buildingId", "roomType" ],
      "description": "A fixed string useful for building information like filter or phases. Used by client applications to define specific behavior for the modelName _TODO: get the full list_"
    },
      "min": {
        "type": "number",
        "description": "Minimum value. Numeric attributes only."
      },
      "max": {
        "type": "number",
        "description": "Maximum value. Numeric attributes only."
      },
      "mostFrequentValues": {
        "type": "array",
        "items": {
          "type": [ "integer", "string" ]
        },
        "description": "Most frequent value, if applicable for this attribute. Truncated to 256 entries."
      },
      "subLayerIds": {
        "type": "array",
        "items": {
          "type": "integer"
        },
        "description": "List of sublayers where this attribute may be found."
      }
    },
  "required": [
    "fieldname",
    "subLayerIds"
  ],
  "esriDocumentation": {
    "examples": [
      {
        "title": "`String` attribute",
        "code": {
          "fieldName": "fournitures",
          "label": "Meuble interieur",
          "mostFrequentValues": [ "chair", "table", "cubicle", "boxes" ],
          "subLayerIds": [ 10, 4, 5 ]
        }
      },
      {
        "title": "`integral` attribute with `modelName` to specify filterable semantic ",
        "code": {
          "fieldName": "floor",
          "label": "Etages",
          "modelName": "floorId",
          "mostFrequentValues": [ 1, 2, 3 ],
          "subLayerIds": [ 10, 15 ]
        }
      },
      {
        "title": "`float` attribute",
        "code": {
          "fieldName": "diameter",
          "label": "Diametre de conduit",
          "min": 0.2566,
          "max": 2.256,
          "subLayerIds": [ 3, 5 ]
        }

      }
    ]
  }
}
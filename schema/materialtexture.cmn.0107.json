{
  "title": "Material texture definition",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "",
  "properties": {
    "textureSetDefinitionId": {
      "type": "integer",
      "description": "index in [layer.textureSetDefinitions](layer.md)"
    },
    "texCoord": {
      "type": "integer",
      "description": "Set if UV coordinates vertex attribute to use. Must `0` or `1`. "
    },
    "factor": {
      "type": "number",
      "description": "_normal texture_: scalar multiplier applied to each normal vector of the normal texture. For _occlusion texture_,scalar multiplier controlling the amount of occlusion applied. Default=`1`"
    }
  },
  "required": [
    "textureSetDefinitionId", "texCoord"     
  ],
  "esriDocumentation": {
    "examples": [

    ]
  }
}
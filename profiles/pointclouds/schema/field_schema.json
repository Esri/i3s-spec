{
    "title": "I3S point cloud scene layer: field",
    "type": "object",
    "$schema": "http://json-schema.org/draft-04/schema",
    "description": "Attribute description as a field",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the attribute. See matching attributeInfo description."
        },
        "type": {
            "type": "string",
            "description": "Describes the type of the attribute. Currently UNUSED for point cloud scene layer."
        },
        "alias": {
            "type": "string",
            "description": "Alias names for the attribute. Currently UNUSED for point cloud scene layer."
        }
    },
    "required": [
        "name",
        "type",
        "alias"
    ]
}
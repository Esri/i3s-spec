{
  "title": "header",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "The header definition provides the name of each field and the value type. Headers to geometry resources must be uniform across a cache and may only contain fixed-width, single element fields.",
  "properties": {
    "property": {
      "type": "string",
      "description": "The name of the property in the header. "
    },
    "type": {
        "type": "string",
        "enum": [ "UInt8", "UInt16", "UInt32", "UInt64", "Int16", "Int32", "Int64", "Float32", "Float64" ],
        "description": "The element type of the header property."
    }
  },
  "required": [
    "type",
    "property"
],
"esriDocumentation": {
    "examples": [
       {
         "title": "Header 3D Object and integrated mesh scene layer",
         "code": {
          "header": [
            {
            "property": "vertexCount",
            "type": "UInt32"
            },
            {
            "property": "featureCount",
            "type": "UInt32"
            }
            ]
        }   
      }
    ]
   }
}
